version: '3.8'

services:
  app:
    build: .
    container_name: e-arogyata-platform
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=production_earogyata_secret_key_2026
    restart: always
    volumes:
      - ./data.json:/app/data.json
